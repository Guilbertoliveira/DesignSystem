import { Meta, Controls, Primary } from "@storybook/blocks"
import * as Story from "./Button.stories.ts"

# Botão

Este é um componente de botão

<Meta of={Story}/>

{/* cria um componente de um formato padrão primary */}
<Primary />  

{/* cria todas as propriedades que estão variando desse componente */}
<Controls />