import { Meta, Controls, Primary, Canvas } from "@storybook/blocks"
import * as Story from "./Button.stories.ts"

{/* essa pagina é a pagina de introdução da documentação */}

# Botão

Este é um componente de botão

<Meta of={Story}/>

{/* cria um componente de um formato padrão primary, sem variações */}
<Primary />  

{/* Canvas pede para ter uma referencia do componente(story, que está no stories) no caso com o of */}
<Canvas of={Story.Violet}/> 
{/* aqui é a pagina que é exibida na documentação */}


{/* cria todas as propriedades que estão variando desse componente */}
<Controls />